{
  "name": "xpendit-bonus-demo",
  "private": true,
  "description": "Optional full-stack demo for the Xpendit rules engine",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix api\" \"npm run dev --prefix web\"",
    "build:engine": "npm run build --prefix ..",
    "postinstall": "npm install --prefix api && npm install --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
